NEXT_PUBLIC_API_URL=http://localhost:9761

# web-ssr has no login form of its own — after logging in here, the profile
# page offers a link that hands the session off to web-ssr's callback route.
NEXT_PUBLIC_SSR_APP_URL=http://localhost:9763
