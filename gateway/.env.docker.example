# Used by docker-compose (gateway/.env.docker) — copy this file to
# gateway/.env.docker before `docker compose up`.
# Service names (Docker Compose's internal DNS), not localhost.
PORT=9760
CSR_TARGET=http://web-csr:9762
SSR_TARGET=http://web-ssr:9763
